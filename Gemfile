source "https://rubygems.org"

# Matches the GitHub Pages build environment (Jekyll version and the
# whitelisted plugins from _config.yml), so a local preview renders
# exactly what production will publish.
gem "github-pages", group: :jekyll_plugins

# `jekyll serve` needs webrick explicitly on Ruby 3+
gem "webrick"
